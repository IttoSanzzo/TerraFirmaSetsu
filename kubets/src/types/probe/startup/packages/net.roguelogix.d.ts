declare module "packages/net/roguelogix/quartz/$QuartzEvent$Startup" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $QuartzEvent$Startup$$Type = ($QuartzEvent$Startup);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $QuartzEvent$Startup_ = $QuartzEvent$Startup$$Type;
}
}

declare module "packages/net/roguelogix/quartz/$QuartzEvent$ResourcesReloaded" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $QuartzEvent$ResourcesReloaded$$Type = ($QuartzEvent$ResourcesReloaded);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $QuartzEvent$ResourcesReloaded_ = $QuartzEvent$ResourcesReloaded$$Type;
}
}

declare module "packages/net/roguelogix/phosphophyllite/mixin/helpers/$IPhosphophylliteLazyOptional" {
import { $LazyOptional } from "packages/net/minecraftforge/common/util/$LazyOptional"
import { $NonNullConsumer } from "packages/net/minecraftforge/common/util/$NonNullConsumer"

/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $IPhosphophylliteLazyOptional$$Type<T> = ($IPhosphophylliteLazyOptional<T> | ((arg0: $NonNullConsumer<$LazyOptional<T>>) => void));
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $IPhosphophylliteLazyOptional_<T> = $IPhosphophylliteLazyOptional$$Type<T>;
}
}

declare module "packages/net/roguelogix/quartz/$QuartzEvent$Shutdown" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $QuartzEvent$Shutdown$$Type = ($QuartzEvent$Shutdown);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $QuartzEvent$Shutdown_ = $QuartzEvent$Shutdown$$Type;
}
}

declare module "packages/net/roguelogix/quartz/$QuartzEvent$FrameStart" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $QuartzEvent$FrameStart$$Type = ($QuartzEvent$FrameStart);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $QuartzEvent$FrameStart_ = $QuartzEvent$FrameStart$$Type;
}
}

declare module "packages/net/roguelogix/quartz/$QuartzEvent$ResourcesLoaded" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $QuartzEvent$ResourcesLoaded$$Type = ($QuartzEvent$ResourcesLoaded);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $QuartzEvent$ResourcesLoaded_ = $QuartzEvent$ResourcesLoaded$$Type;
}
}

declare module "packages/net/roguelogix/phosphophyllite/event/$ReloadDataEvent" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $ReloadDataEvent$$Type = ($ReloadDataEvent);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $ReloadDataEvent_ = $ReloadDataEvent$$Type;
}
}

declare module "packages/net/roguelogix/quartz/$QuartzEvent" {
export {} // Mark the file as a module, do not remove unless there are other import/exports!
/** Class-specific type exported by ProbeJS, use global `{Type}_` types for convenience unless there's a naming conflict. */
export type $QuartzEvent$$Type = ($QuartzEvent);
/** Global type exported for convenience, use class-specific types if there's a naming conflict. */
declare global {
export type $QuartzEvent_ = $QuartzEvent$$Type;
}
}

